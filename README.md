# cf-func-page  
example project for cloudflare pages with functions  
  
https://cf-func-page.pages.dev/  
  
dev:  
```  
make getnode && \
. <(make setnode) && \
make npminstall && \
make startdev
```